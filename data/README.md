# `data/` — the NHS map data pipeline

Everything in here is generated by `scripts/nhs/` and committed on purpose. The
app imports generated TypeScript (`src/lib/venues/nhs-hospitals-data.ts`); these
files are the auditable intermediate that says where that TypeScript came from.

This exists because the hospital directory used to be a 721-row TypeScript file
with no source URL, no script and no date. Nobody could refresh it or check it.

## Why a workflow does the fetching

The upstreams — NHS ODS, postcodes.io, Overpass — aren't reachable from every
development environment (sandboxed and proxied setups routinely block them). CI
has unrestricted egress, so `.github/workflows/nhs-data.yml` runs the pipeline on
a schedule and opens a PR with the results. That keeps the data reproducible
regardless of where anyone is working, and makes every refresh a reviewable diff.

## Running it

```bash
npm run nhs:refresh       # stages 1-5: fetch -> geocode -> footprints -> merge -> generate
npm run nhs:discover      # stage 6: find candidate floor-plan PDFs (writes a triage queue)
npm run nhs:plans         # stage 7: download the PDFs approved in plan-sources.json
```

Individual stages: `nhs:fetch`, `nhs:geocode`, `nhs:osm`, `nhs:build`,
`nhs:venues`. Each writes its own output, so a failure late in the chain doesn't
discard earlier work.

## Files

| File | Written by | What it is |
|---|---|---|
| `raw/ods/*.csv` | `fetch-ods` | ODS bulk extracts, byte-for-byte as published |
| `manifest.json` | all stages | per-source URL, SHA-256, row counts, fetch time |
| `geocode-cache.json` | `geocode` | postcode → lat/lng, accumulated so refreshes are cheap |
| `footprints.geojson` | `fetch-osm` | hospital building outlines from OpenStreetMap |
| `nhs-sites.json` | `build-sites` | **the canonical merged site list** |
| `mapped-coverage.json` | `build-sites` | sites skipped because a fully-mapped venue covers them |
| `mapped-sites.json` | hand-maintained | build config for sheet-derived venues (see below) |
| `trust-websites.json` | `discover-plans` | trust ODS code → website, from the ODS ORD API |
| `plan-candidates.json` | `discover-plans` | **triage queue** of candidate floor-plan PDFs |
| `plan-sources.json` | hand-maintained | the **approved** PDFs `fetch-plans` may download |

`mapped-sites.json` is the single source of truth for both
`scripts/maps/generate-all.mjs` and `scripts/maps/build-venues.mjs`, which
previously each carried their own copy of the same list.

## Site data automates. Floor plans do not.

Sites, coordinates and building footprints reach full national coverage with no
human in the loop. Indoor floor plans don't, and no amount of scripting changes
that: there is no national dataset, so each trust's own PDFs are the only source.

`discover-plans` therefore produces a **queue, not an import**. A person decides
what's usable and moves it into `plan-sources.json`; `fetch-plans` downloads only
what's listed there. That boundary is deliberate — see licensing.

## Licensing

- **NHS ODS** (site register): Open Government Licence v3.0. Attribution
  required — carried in `nhs-sites.json` and in the generated venue file.
- **ONS Postcode Directory** via postcodes.io (coordinates): OGL v3.0.
- **OpenStreetMap** (footprints): ODbL 1.0, © OpenStreetMap contributors.
  Attribution is **required wherever footprints are displayed** — the map
  surfaces it in `src/components/FloorPlanMap.tsx`.
- **Trust site-map PDFs**: the trusts' own copyright, **not** open data. They are
  not mirrored in this repo (`map/` is gitignored); only the derived SVGs the app
  needs ship, and each source is recorded in `plan-sources.json`. Auto-ingesting
  these across every trust in England is not a decision a script should make.
